# CCode → App Handoff
Updated: 2026-06-16T01:00:00Z
Branch/PR: main + open PRs (see below)
Atlas prod: https://atlas.obelisque.io
Atlas main HEAD: 3ad4227 (SS176 fix is live)
Authoritative backlog: `web/docs/BACKLOG.md` (in PR #10)

## Batch execution summary (2026-06-15 → 2026-06-16)

Andrew's three-phase batch + extra guardrail (ADDITIVE only, no destructive ops, no self-merge):

### Phase 1 — Backlog discipline ✅ PR #10
- `web/docs/BACKLOG.md` is the new single authoritative registry. Fields: SS-id, Title, Status (Requested → Active → In Review → Done), Date, Description, Redundancy flag, Interference flag, Caveats.
- `web/AGENTS.md` gets `BEGIN:backlog-discipline` block — non-negotiable rule: reconcile every session, log every new request the turn it appears, redundancy/interference vetted, removal only after Done + ≥7 days live.
- Seeded from cross-source audit (bridge logs + FUTURE_NOTES + 109 parked items + open PRs + SS screenshots).
- **3 AT-RISK items surfaced**: AR1 Colors PR, AR2 Apply-token rotation UI, AR3 Hire→create-employee handoff.

### Phase 2 — Dropdown sort (the lost 2026-06-14 feature) ✅ PR #11
- `web/src/lib/util/sort.ts` — shared util with `sortByName`, `sortAlphaNumeric`, `sortByOrderThenName`. `localeCompare(numeric: true)` so "Bin 2" < "Bin 10".
- Applied to vendors (bills, receive, items, bulk-edit), locations (bills, receive, dashboard incident, waste, waste archive, count, items), waste items, assets.
- **Deliberately NOT touched** per Andrew's caveat: inventory CATEGORY dropdown (display_order honored), spots picker (display_order), schedule locations (sortLocationsForDisplay), all status/category constant arrays.

### Phase 3 — Platform-admin v1 ✅ PR #12
- Mig 123 — `platform_admins` + `platform_admin_actions` tables, helper functions, RLS. ADDITIVE.
- Tiered roles: `owner` (commercial — tenant list + audit log; no business data) vs `support` (full cross-tenant technical, audited). Helpers `user_is_platform_admin` (read) + `user_is_platform_support` (write).
- `/platform` tenant picker + `/platform/[tenantId]/audit` viewer. Non-admins get 404 (not redirect — preserves surface anonymity).
- Bootstrap script `web/scripts/bootstrap-platform-admin.mjs` — idempotent, one-off, seeds andrew@obelisque.io as role=owner.
- **Sequence**: review PR → merge → apply mig 123 → run bootstrap script → visit /platform as andrew@obelisque.io.

## All open PRs (in review)

| PR | Title | Status |
|---|---|---|
| #7  | Schedule gestures + SS179 (timer 350ms) | green CI, mergeable |
| #8  | TO panel + top-area compression + sticky-top + chips-clickable + drop Manage | **partial** — sticky wrapper landed; chips-clickable + drop-Manage **stashed locally** while audit ran. Resume next session |
| #9  | SaaS architecture into AGENTS.md + platform-admin plan v1 (original) | **superseded** by PR #12 which revises the plan doc + builds — close on merge of #12, or merge #9 first as docs-only |
| #10 | **Phase 1 — Backlog registry + AGENTS.md backlog-discipline** | green CI, mergeable, docs-only |
| #11 | **Phase 2 — Dropdown sort util + applied to unordered pickers** | green CI, mergeable |
| #12 | **Phase 3 — Platform-admin v1 (mig 123 + /platform UI)** | green CI, mergeable |

## Cron status (verified)

- Square cron HEALTHY since 2026-06-15 19:38 UTC (Andrew set OWNER_EMAIL=andrew@autumncoffeeroasting.com in Vercel).
- cron_runs ✓ for the 15-min scheduled runs since then.
- No `resolve_tenant` errors in error_log.

## Decisions needed from Andrew

1. **Merge order for PRs #10/#11/#12** — they're independent and can merge in any order. PR #9 is superseded by #12 — close #9 on merge of #12, or merge #9 first as docs-only (either works).
2. **PR #8 follow-up** — resume sticky+chips+drop-Manage next session, or interrupt with something else?
3. **AR1 (Colors PR), AR2 (Apply-token UI), AR3 (Hire→employee handoff)** — surface, park, or wait?
4. **Q11 Square partner SaaS, Q12 sandbox Supabase, Q13 inventory model rebuild** — logged as Requested; Q13 hard-blocked on Q12. No build started.

## Notes

- Bridge confirmed live each session via HTTP 200 check on the raw URL.
- Repo is public — no secrets, no PII.
- Every standing rule from `web/AGENTS.md` is enforced; new requests go straight to `web/docs/BACKLOG.md`.
