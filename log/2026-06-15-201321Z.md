# CCode → App Handoff
Updated: 2026-06-15T20:10:00Z
Branch/PR: main + open PRs (see below)
Atlas prod: https://atlas.obelisque.io
Atlas main HEAD: 3ad4227 (verified deployed; PR #6 SS176 fix is live)

## Caught up — status sync for claude-desktop

### LIVE IN PROD right now
- Schedule matrix base (every cell clickable + location chip + context-aware modal) — PR #4 (`f51e073`)
- TO portal partial-day form + cancel pending — PR #5 (`2b74bf3`) + mig 121
- SS176 RLS fix (`employees_insert_owner_or_controller`) — PR #6 (`3ad4227`) + mig 122. Verified by Andrew through the UI
- Square cron is HEALTHY again after Andrew set OWNER_EMAIL=andrew@autumncoffeeroasting.com in Vercel: cron_runs shows ✓ at 19:38 and 19:45 UTC (manual + scheduled), 8275+ orders upserted, no resolve_tenant errors since

### BUILT LOCALLY — PRs open, awaiting Andrew

**PR #7 — Schedule gestures (single→editor, double→modal)**
- `gestures-v2-instrumented`, sha `d84fd1e`, CI ✅, mergeable
- Debug instrumentation stripped per Andrew's confirmation that double-click works
- Behavior: pill single-click = inline editor / pill double-click = full modal / empty-cell double-click = create modal pre-filled with (employee, date)

**PR #8 — TO warning panel (SS181/182) + schedule top-area compression**
- `ss181-to-warning-panel`, CI ✅, mergeable
- Three-section timing split (This Week red / Upcoming yellow / Pending orange + PENDING chip)
- Compressed per Andrew's followup: inline limit 1 (was 2), tighter padding, top-of-schedule `space-y-4 → space-y-2`, topbar `p-4 → p-2.5`, manage button smaller — claws back significant matrix height

**PR #9 — SaaS architecture into AGENTS.md + platform-admin plan (SIGN-OFF needed)**
- `saas-architecture-platform-admin`
- Writes the 3-identity-level model (Platform / Tenant Owner / Member) into `web/AGENTS.md` so every future session inherits it
- Flags that the current OWNER_EMAIL Square sync is single-tenant pilot mode; SaaS-correct version is per-tenant config + cron loop (build when tenant #2 appears)
- `web/docs/PLATFORM_ADMIN_PLAN.md` is a PROPOSAL ONLY — no migration, no code. Schema for `platform_admins` + `platform_admin_actions` + helper functions + access pattern + UI affordance. **4 yes/no decisions for Andrew at the bottom.** Sign-off required before mig 123

### Architectural corrections this session

- Removed the wrong `tenant_members` row that briefly made `andrew@obelisque.io` an owner of Autumn Coffee. Authoritative model: obelisque.io is the PLATFORM/VENDOR identity, NEVER a tenant_members owner
- Confirmed `andrew@autumncoffeeroasting.com` is the only tenant owner of Autumn Coffee (`role=owner, active=true`, tenant `f7ee8e81-8fc0-4d26-a5ef-864e92688435`)
- OWNER_EMAIL env var in Vercel is correctly set to `andrew@autumncoffeeroasting.com` — cron healthy

### Queue (not yet started)

| Q# | Item | Status |
|---|---|---|
| Q1 | Shopify embed snippet | Queued |
| Q2 | Resume filename preservation | Queued |
| Q3 | Access matrix check/X grid (5-role) | Queued |
| Q4 | VAPID key error fix (POST /schedule, setVapidDetails URL-safe Base64) | Queued |
| Q5 | /audit/errors nav link in Settings (gated owner/controller/auditor) | Queued |
| Q6 | Drag/drop scheduler — apply same gesture model after PR #7 merged | Blocked on PR #7 merge |

### Decisions needed from Andrew

1. **Merge order** — preferred sequence for PR #7, #8, #9. PR #9 has no migration/UI changes (docs only), so safe to merge anytime; #7 and #8 are user-visible and can merge in either order
2. **PR #9 plan sign-off** — 4 yes/no questions inside `web/docs/PLATFORM_ADMIN_PLAN.md` section 6
3. **TO panel pending color** — orange `#f97316` shipped; OK or tweak?
4. **Apply-token rotation UI** — still pending earlier A/B (build or skip)

## Notes for the app
- Update protocol: CCode edits this file → `node scripts/bridge.mjs` → snapshot + push. Bridge confirmed live each session via HTTP 200 check on raw URL
- Repo is **public**: dev-handoff text only — no secrets, no PII
