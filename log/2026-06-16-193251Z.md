# CCode → App Handoff
Updated: 2026-06-16T16:00:00Z
Branch/PR: main + open PRs (see below)
Atlas prod: https://atlas.obelisque.io
Atlas main HEAD: 3ad4227
Authoritative backlog: `web/docs/BACKLOG.md` (in PR #10)
Sandbox loop: `web/docs/SANDBOX.md` (in PR #10)

## This session (2026-06-15 → 2026-06-16)

### Task A — PR #12 defect fix (pushed to platform-admin-v1)
The list_tenants audit insert was silently failing the FK constraint (sentinel UUID '00000000-...' isn't a real tenant). Fixed:
- Mig 123 `target_tenant_id` is now nullable (null = non-tenant-scoped action).
- `listAllTenantsAsPlatformAdmin` passes `target_tenant_id: null` instead of sentinel.
- Both audit-insert call sites (`list_tenants` + `view_audit_log`) now surface their write errors via `logError(source='app')` instead of swallowing. Read still proceeds.
- RLS unchanged — `user_is_owner_or_controller(NULL)` returns false (tenant owners don't see non-tenant-scoped rows); `user_is_platform_admin()` OR branch still sees all.
- Deleted leftover `wire-obelisque-owner.mjs` + `unwire-obelisque-owner.mjs` (obsolete; obelisque.io is platform_admins, not tenant_members).

### Task B — BACKLOG.md chat-side merges (pushed to backlog-registry / PR #10)
Logged only — not built this batch:
- **AR4 SS184** — inventory item modal "Save for reuse" checkbox stays checked after save. Bug.
- **AR5 SS185** — Add/Edit inventory item "Units & Cost" busy layout. Move CONVERSION up, conditional "Save for reuse" (only when Custom + not in catalog), collapse empty space. Same save-clear root as AR4 — bundle.
- **AR6** — Inventory "date price was input" tracking. Bake into Q13 design.
- **Q13 design notes** — full unit-hierarchy model (Purchase → Count/Transfer → Usage), facets, recipes-as-separate-objects, open count-unit-level decision.
- **SCOPE1** Autumn Direct, **SCOPE2** Business analysis dashboard, **SCOPE3** Automotive repair system — logged as separate-scope projects.

### Sandbox plumbing (Q12 — pushed to PR #10)
- `web/.env.branch.local.example` — placeholders for branch creds. Andrew fills `web/.env.branch.local` himself (gitignored).
- `web/.gitignore` — `!.env*.example` exception so placeholder files can be committed.
- `web/scripts/sandbox-validate.mjs` — SAFETY GATE (aborts if prod ref == branch ref), schema parity check with positive control (platform_admins absent proves fork mirrors prod), migration-tracking state, write-path proof (create/insert/select/drop `_sandbox_check`).
- `web/docs/SANDBOX.md` — full loop documented. Bold rule: NEVER use the Supabase dashboard "merge to production" — it bypasses `web/db/`, `_migrations_log`, PR review.

**Validation status**: blocked on Andrew populating `web/.env.branch.local`. Once filled, run `node scripts/sandbox-validate.mjs` and confirm green → Q12 promotes to Done. Until then Q12 stays **Active**.

**Q13 status update**: no longer hard-blocked on Q12 (plumbing exists). Now gated only on the OPEN DESIGN DECISION: is the count/transfer unit always the MIDDLE level, or can ANY level be the count unit?

## All open PRs (in review)

| PR | Title | Status |
|---|---|---|
| #7  | Schedule gestures + SS179 (timer 350ms) | green CI, mergeable |
| #8  | TO panel + top-area compression + sticky-top (partial) + chips-clickable (stashed) + drop Manage (stashed) | **partial** — resume next session |
| #9  | SaaS architecture + platform-admin plan v1 (original) | **superseded** by #12 |
| #10 | Backlog registry + AGENTS.md backlog-discipline + chat-side merges + sandbox plumbing | green CI, mergeable, docs+plumbing only |
| #11 | Dropdown sort util + applied to unordered pickers | green CI, mergeable |
| #12 | Platform-admin v1 (mig 123 nullable target + tiered + /platform UI + audit-insert error surfacing) | green CI, mergeable |

## Cron status (verified)
Healthy. cron_runs ✓ since 2026-06-15 19:38 UTC after Andrew set OWNER_EMAIL=andrew@autumncoffeeroasting.com in Vercel.

## Decisions needed from Andrew

1. **Merge order** for #10/#11/#12 — independent, any order. #9 superseded by #12 (close on merge of #12).
2. **PR #8 follow-up** — resume sticky+chips+drop-Manage next session?
3. **AR1 / AR2 / AR3 / AR4 / AR5 / AR6** triage — surface, park, or wait?
4. **Q12 finalization** — populate `web/.env.branch.local` + run `node scripts/sandbox-validate.mjs` so we can mark Q12 Done.
5. **Q13 OPEN DECISION** — count/transfer unit always middle level, or any-level allowed? Unblocks Q13 design.
6. **SCOPE3 (Automotive repair)** — track in Atlas BACKLOG.md or split to its own backlog?

## Notes
Bridge confirmed live each session via HTTP 200 check on raw URL.
