# CCode → App Handoff
Updated: 2026-06-14T17:30:00Z
Branch/PR: main (Atlas — see per-feature notes)
Atlas prod: https://atlas.obelisque.io
Atlas main HEAD: 2b74bf3 (verified deployed)

## Completed this session
- Autumn Bridge handoff channel established — [LIVE IN PROD] (no PR — direct to main)
- Square sync backfilled by hand (5-day gap from 2026-06-08 → 2026-06-14, 1119 orders) — [LIVE IN PROD]
- Test employee account created so Andrew can act-as employee (`test-employee@autumncoffeeroasting.com`; password in `web/.test-employee-creds`, gitignored) — [LIVE IN PROD]

## Status snapshot (four active workstreams)

### 1. Excel-style schedule editor (Chronos)
- **[LIVE IN PROD]** — PR #4 (`matrix-cells-clickable`), merged as `f51e073`.
- What shipped: every (employee × day) cell is clickable to add a shift; inline editor with location chip strip (incl. single-location employees); modal opened from matrix gets an editable Location dropdown at the top (context-aware).
- What was attempted but **reverted before merge**: gesture reversal (single-click → inline editor / double-click → modal). After 5 attempts on the preview the modal wouldn't reliably appear on dblclick — current prod semantics remain the proven original (single-click → modal, double-click → inline editor).

### 2. Shopify embed snippet
- **[IN PROGRESS]** — no PR yet. Queued as PR #2 in the rolling backlog.
- What's left: deliver a snippet that actually renders the apply form inside a Shopify Custom Liquid block — test against a real Shopify storefront page (not just the iframe approach).

### 3. Resume filename preservation
- **[IN PROGRESS]** — no PR yet. Queued as PR #3 in the rolling backlog.
- What's left: add `resume_original_filename` text column on `applicants`; capture original filename at upload; display it next to the download link in `/applicants`. Storage path stays sanitized.

### 4. Access matrix UI (5-role graduated tiers)
- **[IN PROGRESS]** — spec locked; build pending. No PR yet. Queued as PR #4 in the rolling backlog.
- What's left: replace the tier-name dropdown with a clickable green-check / red-X grid. Threshold-based toggles (clicking sets `min_tier`; higher tiers inherit). One-pass implementation per the approved 5-role matrix (Owner ▸ Controller ▸ Manager ▸ Lead ▸ Employee).

## Also shipped this session (not in the seed)
- **TO portal — SS175** (PR #5, `time-off-employee-tweaks`, merged as `2b74bf3`): [LIVE IN PROD]
  - Partial-day form is mobile-clean (overlapping helper text gone; single-column layout)
  - Employees can now cancel their own pending TO requests (mig 121 RLS policy `time_off_delete_own_pending`)

## Blockers (hard — production-impacting)

- **Vercel cron is silently failing**: `OWNER_EMAIL` env var is missing from the Vercel production project, so `/api/cron/square-sync` 500s on every invocation (cron_runs table is empty since 2026-06-08). Manual backfill done; sustained sync requires the env var to be set. Cannot fix from here (no Vercel CLI on this machine; per Andrew's preference I don't walk him through 3rd-party UIs from memory).
  - **What Andrew needs to do**: add `OWNER_EMAIL=andrew@autumncoffeeroasting.com` to the Vercel production env (Settings → Environment Variables → Production), then redeploy or wait for next cron tick.

- **SS176 — Add Employee RLS error**: the in-app Add Employee form on the Atlas UI returns `new row violates row-level security policy for table "employees"` for owner-role Andrew. Blocks creating any new employee through the UI. Workaround used this session: service-role admin script. Real fix is a separate PR (employees insert policy needs widening for tenant owners; not yet diagnosed root-cause).

## Decisions needed from Andrew (yes/no or A/B)

1. **Gesture reversal on schedule pills** — keep debugging the single-click→editor/double-click→modal mapping in a focused branch with on-page event-log instrumentation, **or** accept the original semantics permanently and move on? (A: debug it / B: keep original)

2. **SS176 RLS fix priority** — fix as the next PR (will block the rolling queue), **or** queue behind Shopify embed + Resume filename + Access matrix? (A: jump the queue / B: stay in order)

3. **Apply token rotation** — there's no in-app UI to rotate `tenants.apply_token` today. Want one as a small follow-up, or live with the DB-side rotation? (A: build UI / B: skip)

## Notes for the app
- Update protocol: CCode edits `ccode-to-app.md` then runs `node scripts/bridge.mjs` (snapshot to `log/`, commit, push to main). App→CCode stays manual.
- Repo is **public**: dev-handoff text only — no secrets, no PII, no internal URLs beyond the production hostname.
- The current test employee credentials live in the Atlas repo at `web/.test-employee-creds` (gitignored) — they are **not** in this public bridge.
