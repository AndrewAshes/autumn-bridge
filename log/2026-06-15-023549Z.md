# CCode → App Handoff
Updated: 2026-06-15T02:30:00Z
Branch/PR: main + open PRs (see below)
Atlas prod: https://atlas.obelisque.io
Atlas main HEAD: 3ad4227 (verified deployed)

## What's LIVE IN PROD right now
- Schedule matrix base (every cell clickable, location chip, context-aware modal) — PR #4 merged `f51e073`
- TO portal partial-day form + cancel pending — PR #5 merged `2b74bf3` + mig 121
- **SS176 RLS fix — PR #6 MERGED** (`3ad4227`) + mig 122 live. Andrew confirmed working through the UI.

## What's BUILT LOCALLY (PRs open, awaiting Andrew)

### PR #7 — Schedule gestures (single→editor, double→modal) ✅ READY TO MERGE
- `gestures-v2-instrumented` — green CI, MERGEABLE, **instrumentation stripped**
- Andrew confirmed double-click→modal works on the preview; debug overlay + all logging hooks removed cleanly
- Gesture behavior shipped:
  - Pill single-click → inline editor (250ms counter-based discrimination)
  - Pill double-click → full modal (Case B counter, or Case A upgrade if editor already open, or onDoubleClick safety net)
  - Empty-cell single-click → inline editor
  - Empty-cell double-click → create modal pre-scoped to (employee, date) with employee pre-filled (SS179)
- No dead instrumentation, no behavior change vs. what Andrew tested

### PR #8 — TO warning panel (SS181/182)
- `ss181-to-warning-panel` — green CI, mergeable
- Horizontal split by TIMING (not status): This Week (red) | Upcoming (yellow) | Pending (orange + PENDING chip) | Manage →
- Fixed footprint by default; tap any section to expand JUST THAT section downward
- Pending requests live in Pending section only (no double-listing)
- Widened page.tsx TO fetch window to +6 months so the panel sees upcoming/pending; matrix offByDate still clips to viewed week (no side effects)
- Matrix bubbles, cell badges, timeline strips — untouched per spec

## Ownership clarification (resolved this session)
- **andrew@autumncoffeeroasting.com** = OWNER of Autumn Coffee tenant. Confirmed: `role=owner, active=true` on tenant `f7ee8e81-8fc0-4d26-a5ef-864e92688435`
- **andrew@obelisque.io** = PLATFORM/DEVELOPER. NOT a tenant_members owner. Earlier this session I incorrectly inserted an owner row for obelisque.io to make the cron work — REVERTED. obelisque.io needs a deliberate platform-admin pattern (separate plan below)
- **rememberbreathing@gmail.com** = test barista (employee tier). Unchanged

## Hard blockers (production-impacting)

### Vercel cron — needs OWNER_EMAIL set correctly
- Cron is healthy when authorized with CRON_SECRET (verified by manual hit: 200 OK, 8262 orders, 12 refunds applied)
- But scheduled invocations still fail until **`OWNER_EMAIL=andrew@autumncoffeeroasting.com`** is set in the Vercel production project
- Earlier value `andrew@obelisque.io` was wrong — that account has no tenant membership and shouldn't (it's the platform account)
- After setting the env var: redeploy, or wait for the next 15-min cron tick

## In progress / queued (NOT live)

| Q# | Item | Status |
|---|---|---|
| Q1 | Shopify embed snippet | Not started |
| Q2 | Resume filename preservation | Not started |
| Q3 | Access matrix check/X grid (5-role) | Not started |
| — | VAPID key error fix (POST /schedule "setVapidDetails must be URL-safe Base64") | Not started, planned for next PR |
| — | /audit/errors nav link in Settings, gated owner/controller/auditor | Not started |
| — | Platform-admin pattern for obelisque.io cross-tenant | Plan needed first |
| — | Drag/drop scheduler — apply same gesture model after PR #7 proven | Blocked on PR #7 |

## Decisions needed from Andrew

1. **PR #6 (SS176 RLS fix)**: test through the UI; if it works, merge
2. **PR #7 (gestures v6)**: open the preview, do a double-click, screenshot the overlay; that tells us whether to strip-and-merge or report-and-stop
3. **PR #8 (TO panel)**: preview-test the new 3-section panel; merge if it matches the SS181/182 layout
4. **Pending-yellow vs pending-orange** in PR #8 — final spec was orange (#f97316). If you want a different orange shade or hue, say so on the preview
5. **Apply-token rotation UI** — still pending your earlier A/B (build or skip). Currently treating as skip per your "stop bolting on extras" feedback

## Notes for the app
- Update protocol: CCode edits this file then runs `node scripts/bridge.mjs` (snapshot + push to main).
- Repo is **public**: dev-handoff text only — no secrets, no PII.
