# CCode → App Handoff
Updated: 2026-06-17T21:30:00Z
Branch/PR: main + open PRs (see below)
Atlas prod: https://atlas.obelisque.io
Atlas main HEAD: 3a583e0 (after PR #13 merge)
Authoritative backlog: `web/docs/BACKLOG.md` (LIVE)

## This session — merged to live

| PR | Title | Result |
|---|---|---|
| #16 | BACKLOG.md — capture Q14-Q22 + Q13 downgrade + task taxonomy | **MERGED** → `4fc015c` |
| #14 | **URGENT** Square sync resilient chunking + partial-sync distinguished | **MERGED** → `aa334ae`, **verified live**: `chunksFailed=0`, 8271 orders upserted clean |
| #13 | Cash deposits — freshness + period preset + drift copy + spec + sync-time-keyed stale logic | **MERGED** → `3a583e0` |

PR #13 had a merge conflict on BACKLOG.md (its older snapshot vs. main's Q14-Q22 additions). Resolved by taking main's version (the newer, more complete entries). CI re-greened post-rebase.

## URGENT fix verified live
Manual cron test post-deploy returned `chunksFailed: 0` on 8276 orders fetched / 8271 upserted. The 100-row chunks complete under statement_timeout cleanly. Ethan's Gresham-style "short until manual refresh" symptom should self-heal on every 15-min tick going forward. If any future chunks fail, you'll see them in `/audit/errors` as `phase=partial_upsert` + severity=warn (distinct from full-failure `phase=run` + critical).

## Open PRs

| PR | Title | Status |
|---|---|---|
| #7  | Schedule gestures + SS179 (350ms) | green CI |
| #8  | TO panel sticky/chips (stashed locally; resume next session) | partial |
| #15 | **Q14 Inventory facets + Q14 RECONCILIATION** — mig 124 backfills facets from legacy pricing_model/msrp_cents; form removes legacy controls; facets are canonical | green CI, **left open per Andrew (touches pricing, his review)** |

### What PR #15 now contains (after the reconciliation push)

- **Mig 124 ADDITIVE** + backfill: `UPDATE is_sold=true WHERE pricing_model='msrp'`; `UPDATE is_recipe_component=true WHERE pricing_model='recipe'`; `UPDATE retail_price_cents = msrp_cents WHERE pricing_model='msrp' AND msrp_cents IS NOT NULL`. Idempotent.
- **Form**: removed the legacy "Pricing model" radio block + the legacy "Item sold retail / MSRP" suggestion section. Facets section is the single source. `resolveMsrp` / `formatStrategy` imports dropped (msrp.ts module stays for future).
- **Save payload**: stops writing `msrp_cents` / `pricing_model`. On edit, preserves existing values (no stomp). On create, passes safe defaults.
- **DB columns stay** (`pricing_model`, `msrp_cents`) per your "keep for safety" rule. Only the reads/writes moved.

Test expectation: an existing `pricing_model='msrp'` item with `msrp_cents=350` shows **Sold at retail = on**, **mode = Manual price**, **$3.50** after merge + mig apply. A `pricing_model='recipe'` item shows **Recipe component = on**.

## Cron + platform-admin status
- Square cron HEALTHY with resilient chunking shipped.
- Platform-admin v1 LIVE.

## Captured in BACKLOG.md (NOT built this session)
Q16 TO modal, Q17 Task model (EOW priority, multi-PR), Q18-Q21 quick wins, Q22 Square OAuth (auth-sensitive).

## Decisions needed from Andrew
1. **PR #15** review + merge (then apply mig 124 to prod)
2. **PR #7** gestures merge timing
3. **PR #8** sticky+chips — resume next session?
4. Q16/Q17 vs Q18-Q21 priority for next session
